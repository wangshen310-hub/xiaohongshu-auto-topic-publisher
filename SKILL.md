---
name: xhs-auto-topic-publisher
description: 为小红书图文内容完成不限主题的差异化选题、视觉参考拆解、图片生成质检、文案交付及 Playwright Edge 自动发布。适用于候选选题、整套图文创作或发布既有成稿。
---

# 小红书图文全自动选题发布

按用户请求选择最小必要模式：

- **快速选题**：提供4个差异显著的候选方向、参考链接和视觉拆解；用户要求先审核时不得提前生图。
- **完整创作**：完成选题、3至4张9:16无损PNG、标题、正文、话题、评分循环、对抗审查和交付文档。
- **仅发布**：读取已审核成稿，通过 Playwright + Microsoft Edge 发布，不擅自重写内容。

题材不设预置范围，可根据用户目标自由选择艺术、生活、旅行、知识、产品、时尚、自然、科幻等合法主题。选题必须与用户受众和内容目标相关，不得为追求差异而偏离需求。

默认不抓取或分析小红书账号、笔记和互动数据。外部原型优先调用 `$agent-reach`，先运行 `agent-reach doctor --json`，再通过其 Exa 搜索通道只读检索 Pinterest，收集预览、链接和可观察特征；不得绕过 Pinterest 权限或批量下载受保护原图。`agent-reach` 不可用或 Pinterest 无法访问时，如实报告并等待用户决定是否采用其他可验证来源，不伪造结果。

## 快速选题

1. 检查项目 `outputs` 与历史学习记录，提取近期作品的主题、媒介、主色、构图、光线、焦点、情绪和叙事结构。
2. 使用 `$agent-reach` 至少执行两组差异明显的 Pinterest 查询，收集4至8张候选，再筛选至少4张媒介、情绪、色彩和构图明显不同的参考原型，逐张拆解构图骨架、焦点、留白、光线、色彩、材质、节奏和思想内核。
3. 每个候选包含：主题名、核心命题、目标受众、参考图、视觉语法、系列分镜设想、与近作差异、传播切入点和潜在风险。
4. 用户只要求候选或明确要求先审核时，到此停止并等待选择。

## 完整创作

1. 从选定原型借鉴设计语法，不复制具体场景、人物、建筑、图案或受保护角色；形成原创主题与叙事逻辑。
2. 默认生成3至4张9:16竖图、原始高质量PNG。每张承担不同构图或叙事功能，不能只是替换场景、颜色或主体。
3. 生成前后读取并执行 [质量量表](references/quality-rubric.md)。每张图都必须达到参考图总分及五个核心分项；未达标时只针对缺陷返修并重评。
4. 标题不超过20字；正文简短、具体、有真实观点；话题与作品准确匹配，不声称来自未经验证的平台趋势。
5. 在项目 `outputs` 保存最终PNG和 `主题名_YYYY-MM-DD.md`。简洁文档只包含标题、正文、话题和组图顺序。裁切、放大或格式转换必须如实记录。
6. 对抗性审查检查构图、原创性、材质、空间逻辑、生成瑕疵、缩略图辨识度、系列差异、廉价感和文案AI腔。不得擅自修改用户已经通过或锁定的旧作品。

## 发布

发布属于外部写入动作。只有用户明确要求发布时才能执行；“准备发布”不等于授权提交。

1. 发布前读取 [Edge发布规程](references/edge-publishing.md)。
2. 使用 Playwright 与 Microsoft Edge 的专用持久化用户目录；不得读取或输出Cookie、Token、密码和验证码。
3. 先检查当日日志和笔记管理页。同一标题或同组图片只提交一次，发现重复立即停止。
4. 仅上传最终PNG，按文档核对图片顺序、首图、标题、正文、话题、可见范围和页面错误。若平台提供AI生成内容声明，按真实情况选择。
5. 提交前截图，只点击一次。只有成功提示、`published=true` 跳转、笔记管理页记录或有效作品ID/链接才算提交成功。
6. 显示“审核中”时报告“已提交，审核中”；不得声称已经公开可见。
7. 遇到登录失效、验证码、风控、实名要求、上传失败、字段不确定或无法确认成功时立即停止，不得重复提交或绕过验证。

使用 `scripts/publish_xhs_edge.js --config <json> --prepare` 仅填充与校验；获得明确发布授权后才使用 `--publish`。
